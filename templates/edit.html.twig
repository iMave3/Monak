{% extends "/base.html.twig" %}
{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('tags/contact.css') }}">
{% endblock %}
{% block leftBody %}{% endblock %}

{% block body %}
	{{ form_start(form) }}
	{{ form_widget(form) }}

	<button type="submit">Uložit</button>

	<div>
	prechozi img
		<img src="{{ asset(tag.imageURL) }}" alt="{{ tag.name }}">
	</div>
	{{ form_end(form) }}
{% endblock %}
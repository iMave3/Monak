{% extends "base.html.twig" %}

{% block stylesheets %}
		<link rel="stylesheet" href="{{ asset('css/products.css') }}">
{% endblock %}

{% block body %}
	{% if category.subcategories is defined %}
		{% for subcategory in category.subcategories %}
			<a href="{{ path('catalog', {'name': subcategory}) }}">{{subcategory}}</a>
		{% endfor %}
	{% endif %}

	{% if category.tags is defined %}
		{% for tag in category.tags %}
			<a href="{{ path('tag', {'name': tag}) }}">{{tag}}</a>
		{% endfor %}
	{% endif %}

	{# <div id="right-offer">
		<div id="mainRight-offer">
	{% for product in tag.products %}
        <a href="../php/rucBrouseni.php">
				<img src="{{ asset('img/products/' ~ product.imageURL) }}" alt="">
				<h2>{{ tag.name }}</h2>
				<p>Otevřít</p>
			</a>
	{% endfor %}
		</div>
	</div> #}

{% endblock %}
